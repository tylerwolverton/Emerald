Entity targetEntity = null

State FollowTarget
{
    OnUpdate()
    {
        if(targetEntity == null)
        {
            return
        }    
    
        parentEntity.position = targetEntity.position
    }

    // Fit within bounds of map
}