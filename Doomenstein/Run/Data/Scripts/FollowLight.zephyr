Entity target = null
Vec3 rotationCenter = Vec3( 0,0,0 )

State Loop
{
    OnUpdate()
    {
        MoveInCircle( center: rotationCenter, radius: 2, speed: .25 )
    }
}

State Follow
{
    // parentEntity.position = target.position
    // parentEntity.forwardVec = target.forwardVec
}